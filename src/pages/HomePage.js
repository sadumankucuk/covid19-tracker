import React from 'react';
import WorldMap from '../components/WorldMap';

const HomePage = () => {
    return (
        <div>
            <WorldMap />
        </div>
    );
};

export default HomePage;